# SKILL_NAME

> One line: what you get and when to reach for it.

**What it does.** 2-3 sentences a non-author can understand.

**Install.** Copy the folder into your Claude Code skills directory:
```bash
cp -r skills/SKILL_NAME ~/.claude/skills/
```
Then trigger it by describing the task in your own words (see the `description`
in [`SKILL.md`](SKILL.md) for the phrases it listens for).

**Example.** Show one concrete before → after, or link a sample output.

**Dependencies.** None / list them. Note if any step needs an API key, and
whether there's a no-key path.
